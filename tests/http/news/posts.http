POST http://{{host}}/getPosts
Accept: application/json, text/plain, */*

> {%
    client.test("Request executed successfully", function() {
        // client.assert(response.status === 404, "Response status is not 404");

        client.log(response.body);
    });
%}

###

POST http://{{host}}/news/getDateValidRanges
Accept: application/json, text/plain, */*

> {%
 client.test("Data fetched", function () {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert(typeof response.body.start === 'string', 'Response `start` field is not a string');
        client.assert(typeof response.body.end === 'string', 'Response `end` field is not a string');
 });
%}
